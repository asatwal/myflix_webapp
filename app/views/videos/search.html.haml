%article.video_category
  %header
    %h3= "Results for videos searched by: #{@search_term}"
  .videos.row
    - @videos.each do |video|
      .video.col-sm-2
        = link_to video, nil do
          %img(src="#{video.small_cover_url}")
